---
title: fork创建子进程
tags:
  - linux
  - 系统编程
  - 课时笔记
  - 系统编程/进程
---
# fork创建子进程

## 本节学什么

- `fork`
- 父子进程返回值差异
- 写时拷贝




## 本节学什么详解

- `fork`：`fork` 解决的是“怎样从一个进程复制出一个子进程”这个问题。它是后面 `fork-exec-waitpid` 模型的起点，也是理解父子进程关系、进程并发和资源继承的基础。
- 父子进程返回值差异：`fork` 之后父子进程会得到不同的返回值：父进程拿到子进程 PID，子进程拿到 0，失败时返回 -1。这个差异正是区分执行分支的关键。
- 写时拷贝：写时拷贝解决的是“为什么 `fork` 看起来复制了整个进程，但实际开销没有想象中那么大”这个问题。它解释了现代系统如何延迟真正的内存复制，也是理解 `fork` 效率的关键。

## 知识点补充
- `fork` 会复制当前进程，父子进程从同一位置继续执行。
- 它们返回值不同，因此可以用返回值区分父进程和子进程。
- 现代 Linux 中常结合写时复制理解它的效率。

## 本节内容速览

- `fork` 成功后父子进程从同一位置继续执行
- 父进程拿到子 PID，子进程拿到 0
- 父子逻辑通常靠返回值分支




## 复习时要回答

- fork到底解决什么问题？能不能用自己的话把它讲清楚？
- 父子进程返回值差异和前后内容是什么关系，为什么这一节要把它单独拿出来讲？
- 如果你只允许保留一句话，这一节最该记住的结论是什么？

## 细节补充

- 进程不是程序本身，而是程序运行后形成的实体
- PCB 记录了进程调度、资源和状态，是理解进程的关键
- `fork` 后父子进程从同一位置继续执行，但返回值不同
- 写时拷贝让父子进程先共享物理页面，直到发生修改
- `exit` 和 `_exit` 的区别在 fork 场景里尤其重要

## 本节关键函数

- [[linux系统编程/函数笔记/进程控制/fork]]



## 关联模块

- [[linux系统编程/04 进程基础]]


