---
title: 水平触发与边沿触发
tags:
  - linux
  - 网络编程
  - 概念词条
---
# 水平触发与边沿触发

## 它是什么

- 水平触发和边沿触发是 epoll 通知就绪事件的两种方式。

## 水平触发

- 只要 fd 仍然处于就绪状态，`epoll_wait` 就会反复通知。
- 默认模式通常是水平触发。
- 写法相对安全，适合初学阶段。

## 边沿触发

- 只有状态从未就绪变为就绪时才通知一次。
- 常配合 [[linux网络编程/概念词条/非阻塞I O|非阻塞I/O]] 使用。
- 如果一次没有把数据读完，后续可能不会再次收到通知。

## 易错点

- 边沿触发不是简单地“更快”，它对代码正确性要求更高。
- 使用边沿触发时，通常要循环读到 [[linux网络编程/概念词条/EAGAIN与EWOULDBLOCK|EAGAIN]]。
- LT模式：
	水平触发 -- 默认采用模式。
	缓冲区剩余未读尽的数据会导致 epoll_wait 返回。
- ET模式：
	边沿触发：
	缓冲区剩余未读尽的数据不会导致 epoll_wait 返回。 新的事件满足，才会触发。
	struct epoll_event event;
	event.events = EPOLLIN | EPOLLET;


## 常见出现位置

- [[linux网络编程/课时笔记/05 IO多路复用/03 epoll|03 epoll]]
- [[linux网络编程/概念词条/epoll模型|epoll模型]]
- [[linux网络编程/概念词条/非阻塞I O|非阻塞I/O]]
