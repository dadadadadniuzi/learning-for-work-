---
title: fork-exec进程模型
tags:
  - linux
  - 系统编程
  - 课时笔记
  - 系统编程/进程控制
---
# fork-exec进程模型

## 本节学什么

- `fork + exec + waitpid`
- 父子进程职责分离
- `system` 的定位




## 本节学什么详解

- `fork + exec + waitpid`：这条组合是典型父子协作模型：`fork` 先复制出子进程，子进程用 `exec` 跑新程序，父进程用 `waitpid` 等待并回收。
- 父子进程职责分离：这一部分解决的是“为什么要让父进程负责创建和回收，子进程负责切换程序并执行任务”这个问题。它把前面分散学到的 `fork`、`exec`、`waitpid` 统一成一个稳定模型。
- `system` 的定位：`system` 解决的是“怎样用更省事的方式执行一条 shell 命令”这个问题。它本质上是对 `fork + exec + wait` 的封装，所以这节要把它放在完整模型里看，而不是当成独立主角。

## 知识点补充
- 标准做法是 `fork` 后让子进程 `exec` 新程序，父进程 `waitpid` 回收。
- 这是启动子任务、执行外部命令和封装系统工具的基础套路。
- `system` 只是这个模型的便利包装，不是底层主角。

## 本节内容速览

- 父进程负责创建和回收
- 子进程负责切换成目标程序
- `system` 更像方便入口，不是底层主角




## 复习时要回答

- fork + exec + waitpid到底解决什么问题？能不能用自己的话把它讲清楚？
- 父子进程职责分离和前后内容是什么关系，为什么这一节要把它单独拿出来讲？
- 如果你只允许保留一句话，这一节最该记住的结论是什么？

## 细节补充

- `wait` 是基础回收，`waitpid` 是更灵活的指定回收
- [[linux系统编程/概念词条/waitpid选项|WNOHANG]] 是非阻塞回收里最常见的选项
- `exec` 成功后不会返回，所以成功路径和失败路径要分开写
- `execl/execlp` 和 `execv/execvp` 的差别在于参数组织方式
- `fork + exec` 是创建新程序的标准套路

## 本节关键函数

- [[linux系统编程/函数笔记/进程控制/fork]]
- [[linux系统编程/函数笔记/进程控制/execvp]]
- [[linux系统编程/函数笔记/进程控制/waitpid]]
- [[linux系统编程/函数笔记/进程控制/system]]



## 关联模块

- [[linux系统编程/05 进程控制与回收]]


